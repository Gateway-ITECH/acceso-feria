# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains all required environment variables for the application.
# Copy this file to .env and fill in the appropriate values.

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Application stage/environment (dev, staging, prod)
STAGE=dev

# Server port (default: 3000)
PORT=3000

# API host URL (used for CORS and external references)
HOST_API=

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL database connection settings
DB_HOST=
DB_PORT=5438
DB_USERNAME=
DB_PASSWORD=
DB_NAME=

# =============================================================================
# JWT AUTHENTICATION
# =============================================================================
# JWT secret key for token signing (REQUIRED - use a strong, random string)
# Example: openssl rand -base64 32
JWT_SECRET=

# JWT token expiration time (default: 24h)
# Supported formats: 60s, 10m, 2h, 7d
JWT_EXPIRES_IN=24h

# =============================================================================
# WEBSOCKET NOTIFICATION SERVICE
# =============================================================================
# Webhook API token for external applications to send notifications
# This token must be included in the Authorization header as "Bearer <token>"
# SECURITY: Use a strong, random token and keep it secure
# Example: openssl rand -base64 32
WEBHOOK_API_TOKEN=

# WebSocket CORS origin (default: * for development, specify domains for production)
# For production, specify allowed origins: https://yourdomain.com,https://anotherdomain.com
WS_CORS_ORIGIN=*

# WebSocket namespace (default: /notifications)
# Change this if you need a different namespace for your WebSocket connections
WS_NAMESPACE=/notifications